# ICA maintenance scripts

Scripts in this directory are generic maintenance utilities. Supply project,
collection prefix, namespace, ICA base URL and storage locations explicitly;
do not add real environment defaults or cross-dataspace inventories to source.

Use `--help` for each command. Start destructive operations in dry-run/read-only
mode, resolve the exact target and keep environment-specific command examples in
the operations documentation outside this repository.
